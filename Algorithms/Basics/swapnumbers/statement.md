The goal is to swap two numbers (i.e., exchange their values) without using a temporary or third variable.

Formally,
Given two integers a and b, after the swap:

        a'=b, b'=a

You must perform this only  using bitwise operation.